{% extends "_layout" %}

{% block content %}

    {# Get blocks Data #}
    {% set blocks = entry.landingBlock.all() %}
    {% include 'sections/_landing-block' with { blocks: blocks } %}

    <section class="sib-landing-intro">
        <div class="container">
            <div class="row">
                <div class="col-12">
                    {{ entry.landingIntro }}
                </div>
            </div>
        </div>
    </section>

{% endblock %}
