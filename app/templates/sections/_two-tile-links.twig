{# Get blocks Data #}
{% set blocks = entry.twoTileLinks.all() %}
{% set blocksData = [] %}
{% for block in blocks %}
    {% set blockData = {
        link: block.entry.one.url,
        icon: 'icon',
        title: '',
        subTitle: block.sibTilte,
        text: block.sibText,
        buttonText: block.buttonText,
        background: block.background.one is not null ? block.background.one.url : '/images/DH.jpg'
    } %}

    {% set blocksData = blocksData|merge([blockData]) %}
{% endfor %}

{% include 'sections/_half-link' with { blocks: blocksData } %}
